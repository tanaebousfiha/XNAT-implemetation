Um den Localhost zu starten muss man diese Befehl im Terminal eingeben :

 1) cd ~/dockerfile/xnat-osa/xnat-docker-compose

2) docker-compose up -d
3) docker ps


Based on the steps from the web site XNAT Documentation :
https://wiki.xnat.org/documentation/installing-pipelines-in-xnat#InstallingPipelinesinXNAT-InstallingthePipelineExecutable


to install the Pipeline in xnat :
1. SSH into your Tomcat instance using your command line tool of choice, sudo as the user account that owns your XNAT webapp, and go to the /tmp folder.

2. Create a new directory in /tmp called modules, then enter that directory. 

CODE
$ mkdir modules
$ cd modules

3. Clone your pipeline repository into this folder. This demo uses the DicomToNifti pipeline as an example.
$ hg clone https://bitbucket.org/nrg_customizations/nrg_pipeline_dicomtonifti


4. Navigate back to the /tmp folder, then clone the xnat-pipeline repo into this folder, then enter that directory.

CODE
$ cd ..
$ git clone https://github.com/NrgXnat/xnat-pipeline-engine.git
$ cd xnat-pipeline-engine


5. Copy the provided sample gradle properties file into a working gradle.properties file, then open that file in an editor. 

CODE
$ cp sample.gradle.properties gradle.properties
$ vi gradle.properties

 in The Dokumenation we have to use vi 
i uesd the nano because easier 
activated the :
destination=/data/xnat/pipeline 
modulePaths=/tmp/modules


 Rebuild the Pipeline Engine: ./gradlew